# Image Prompt: Post 6 — Salesforce Has a New Interface — This One Is for AI Agents

**Post**: 6
**Source variant**: technical
**Visual concept**: Mapping/Translation
**Generated**: 2026-04-02

## Core Message (for visual)

Salesforce already has interfaces for humans (page layouts, screen flows, list views) — salesforce-mcp-lib adds a parallel interface for AI agents (MCP tools, resources, prompts) that reuses the same business logic.

## Gemini Prompt

Create a LinkedIn infographic (1200x627px, landscape).

**Visual concept**: A single Salesforce org shown as a central building/system with two parallel interface layers branching out from it — one side for humans, one side for AI agents. Each side has matching pairs connected by dotted lines through the central org, showing that the same business logic powers both interfaces.

**Layout**:
- Left side (labeled "Human Interface"): Three rounded rectangles stacked vertically, each with a small user icon, labeled "Page Layout", "Screen Flow", "List View"
- Center: A single bold vertical bar or column representing the Salesforce org, labeled "Business Logic" at the top. Three horizontal dotted connector lines pass through it, linking each left item to its right counterpart. A small equals sign or bidirectional arrow sits on each connector line inside the center column, reinforcing "same logic."
- Right side (labeled "AI Agent Interface"): Three rounded rectangles stacked vertically, each with a small robot/circuit icon, labeled "SOQL Tool", "Subflow Tool", "tools/list". Styled in the same shape as the left side but with a different accent color.
- Bottom center: A subtle tagline line reading "salesforce-mcp-lib" in a clean monospace font.

**Style**: Dark charcoal background (#1A1A2E). Left side rectangles use warm gray (#4A4A5A) with white text and a small blue (#0176D3) user silhouette icon. Right side rectangles use Salesforce blue (#0176D3) with white text and a small white robot/chip icon. Center column is a slightly lighter charcoal (#2A2A3E) with a subtle blue glow border. Connector lines are dashed, light blue (#5EB1EF). All text is white, sans-serif, clean. The overall mood is technical, precise, and modern — like a well-designed architecture slide.

**Text labels** (exact text to include):
- "Human Interface" (left header)
- "AI Agent Interface" (right header)
- "Page Layout" (left row 1)
- "Screen Flow" (left row 2)
- "List View" (left row 3)
- "SOQL Tool" (right row 1)
- "Subflow Tool" (right row 2)
- "tools/list" (right row 3)
- "Business Logic" (center column header)
- "salesforce-mcp-lib" (bottom tagline)

**Do NOT include**: No photographs of people, no generic tech stock imagery, no clipart, no Salesforce logo or cloud icon (trademark), no code screenshots, no gradients that make text hard to read.

## Alt Text (for LinkedIn)

Infographic showing two parallel interface layers into a Salesforce org: on the left, human interfaces (Page Layout, Screen Flow, List View) and on the right, AI agent interfaces (SOQL Tool, Subflow Tool, tools/list), connected through the same central business logic layer. Labeled "salesforce-mcp-lib" at the bottom.

## Usage Notes

- **LinkedIn image specs**: 1200x627px recommended for feed posts
- **Fallback**: If generation fails, use the alt text as a text-only post header
- **Regeneration**: Adjust the prompt and re-run if the visual doesn't match the post's tone
