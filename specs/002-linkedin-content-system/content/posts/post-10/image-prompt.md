# Image Prompt: Post 10 — Per-User OAuth Now Supported

**Post**: 10
**Source variant**: founder
**Visual concept**: Before/After Split
**Generated**: 2026-04-13

## Core Message

salesforce-mcp-lib now supports per-user OAuth 2.0 Authorization Code with PKCE — each user authenticates with their own Salesforce identity instead of sharing a single service account.

## Gemini Prompt

Create a clean, minimal LinkedIn infographic (1200×627px, landscape orientation).

**Background**: Solid dark navy (#0F172A).

**Layout**: A centered before/after split with a subtle vertical divider line in the middle.

**Top center**: A header strip with the text "Per-User OAuth Now Supported" in white (#FFFFFF), bold, large sans-serif font.

**Left side — "Before"**:
- Small label "Before" in muted gray (#94A3B8) at the top of this column.
- One rounded rectangle pill in charcoal (#1E293B) with a single generic user/gear icon and the label "Service Account" in white.
- A single Salesforce-blue (#0176D3) arrow pointing down to a rounded rectangle labeled "Salesforce Org" in white on a darker panel (#162033).

**Right side — "Now"**:
- Small label "Now" in Salesforce blue (#0176D3) at the top of this column.
- Three rounded rectangle pills in charcoal (#1E293B), stacked vertically with small spacing, each with a generic user icon and labels "User A", "User B", "User C" in white.
- Three separate Salesforce-blue (#0176D3) arrows, one from each user pill, pointing down to a single rounded rectangle labeled "Salesforce Org" in white on a darker panel (#162033).
- A small pill below the arrows with text "Own profile · Own permissions · Own sharing rules" in muted gray (#94A3B8), italic.

**Center divider**: A thin vertical dashed line in dark gray (#334155) separating the two sides.

**Bottom center**: Footer text "salesforce-mcp-lib" in muted gray (#64748B), small sans-serif font.

**Style**: Professional enterprise slide aesthetic. No people, no photographs, no Salesforce logo, no code screenshots, no clipart. Only geometric shapes, icons, and text. Clean spacing, rounded corners on all rectangles.

## Alt Text

Before-and-after diagram showing salesforce-mcp-lib per-user OAuth support. Left side shows a single service account connecting to a Salesforce org. Right side shows three individual users (User A, User B, User C) each connecting with their own identity, inheriting their own profile, permissions, and sharing rules.

## Usage Notes

- Copy the Gemini Prompt section and paste directly into Gemini image generation
- Use the generated image as the LinkedIn post thumbnail for post 10
- Alt Text section is ready to paste into LinkedIn's image alt text field
