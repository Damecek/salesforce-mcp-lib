/**
 * Abstract base class for all JSON-RPC result types.
 *
 * Subclasses define the typed fields that are serialized into the
 * {@code result} member of a successful JSON-RPC response.
 */
public abstract class JsonRpcResultBase {
    // Empty base -- subclasses define typed result fields.
}
