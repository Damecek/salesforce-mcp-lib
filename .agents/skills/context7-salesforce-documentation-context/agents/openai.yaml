interface:
  display_name: "Context7 Salesforce Documentation Context"
  short_description: "Use Context7 to verify sfdocctx docs via a fixed libraryId"
  default_prompt: "Use Context7 to verify and retrieve info from /damecek/salesforce-documentation-context. Always call mcp__context7__query-docs with that fixed libraryId and skip resolve-library-id. Do not assume the current workspace is related to that repository; treat it as an external documentation library."
